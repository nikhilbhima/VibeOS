# VibeOS Environment Variables

# GitHub token for MCP server (optional)
GITHUB_TOKEN=

# Anthropic API key (if using direct API instead of Claude Code CLI)
# ANTHROPIC_API_KEY=

# Development settings
NODE_ENV=development
